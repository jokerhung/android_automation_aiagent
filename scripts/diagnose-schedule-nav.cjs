const{chromium}=require('../vendor/ws-scrcpy-web/node_modules/playwright');(async()=>{const b=await chromium.launch({headless:true,executablePath:process.env.LOCALAPPDATA+'/ms-playwright/chromium-1234/chrome-win64/chrome.exe'}),p=await b.newPage();const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://127.0.0.1:3000',{waitUntil:'networkidle',timeout:20000});const button=p.locator('.scheduleNav');console.log('before',await button.count(),await button.isVisible());await button.evaluate(e=>e.click());await p.waitForTimeout(1000);console.log(JSON.stringify({panel:await p.locator('.schedulePanel').count(),text:(await p.locator('body').innerText()).slice(0,500),errors}));await b.close()})().catch(e=>{console.error(e);process.exit(1)});