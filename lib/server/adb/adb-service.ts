
import { spawn } from "node:child_process";
export class AdbError extends Error{constructor(message:string,public code="ADB_ERROR",public stderr=""){super(message)}}
export type CommandResult={stdout:Buffer;stderr:Buffer};
class SerialQueue{private chains=new Map<string,Promise<unknown>>();run<T>(serial:string,job:()=>Promise<T>):Promise<T>{const previous=this.chains.get(serial)??Promise.resolve();const next=previous.catch(()=>undefined).then(job);this.chains.set(serial,next);return next.finally(()=>{if(this.chains.get(serial)===next)this.chains.delete(serial)})}}
export class AdbService{private queue=new SerialQueue();constructor(private executable=process.env.ADB_PATH||"adb"){}run(args:string[],options:{serial?:string;timeoutMs?:number;signal?:AbortSignal;queued?:boolean}={}):Promise<CommandResult>{const full=options.serial?["-s",options.serial,...args]:args;const execute=()=>this.spawn(full,options.timeoutMs??15000,options.signal);return options.serial&&options.queued!==false?this.queue.run(options.serial,execute):execute()}private spawn(args:string[],timeoutMs:number,signal?:AbortSignal){return new Promise<CommandResult>((resolve,reject)=>{const child=spawn(this.executable,args,{windowsHide:true,stdio:["ignore","pipe","pipe"]});const stdout:Buffer[]=[];const stderr:Buffer[]=[];let settled=false;let timer:NodeJS.Timeout;const abort=()=>{child.kill();finish(new DOMException("Operation aborted","AbortError"))};const finish=(err?:Error)=>{if(settled)return;settled=true;clearTimeout(timer);signal?.removeEventListener("abort",abort);if(err)reject(err)};child.stdout.on("data",d=>stdout.push(d));child.stderr.on("data",d=>stderr.push(d));child.on("error",e=>finish(new AdbError(e.message,e.message.includes("ENOENT")?"ADB_NOT_FOUND":"ADB_SPAWN")));child.on("close",code=>{if(settled)return;settled=true;clearTimeout(timer);signal?.removeEventListener("abort",abort);const out=Buffer.concat(stdout),err=Buffer.concat(stderr);if(code===0)resolve({stdout:out,stderr:err});else reject(new AdbError(err.toString("utf8").trim()||("adb exited with code "+code),"ADB_COMMAND",err.toString("utf8")))});signal?.addEventListener("abort",abort,{once:true});timer=setTimeout(()=>{child.kill();finish(new AdbError("ADB command timed out after "+timeoutMs+"ms","ADB_TIMEOUT"))},timeoutMs)})}async text(args:string[],options:{serial?:string;timeoutMs?:number;signal?:AbortSignal;queued?:boolean}={}){return (await this.run(args,options)).stdout.toString("utf8").trim()}}
export const adbService=new AdbService();
