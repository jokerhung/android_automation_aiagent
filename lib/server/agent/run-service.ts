import "@/lib/server/server-guard";import {deviceManager} from "@/lib/server/adb/device-manager";import {sessionRepository} from "@/lib/server/persistence/session-repository";import {agentRunner} from "@/lib/server/agent/agent-runner";export class RunService{async createAndStart(input:{conversationId:string;deviceSerial:string;goal:string;maxSteps:number;source:"manual"|"schedule";scheduleOccurrenceId?:string}){await deviceManager.requireConnected(input.deviceSerial);const run=sessionRepository.createRun(input.conversationId,input.deviceSerial,input.goal,input.maxSteps);try{agentRunner.start(run);sessionRepository.addMessage(input.conversationId,"user",input.goal);return run}catch(error){sessionRepository.deleteRun(run.id);throw error}}}export const runService=new RunService();
