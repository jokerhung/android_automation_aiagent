node_modules/
dist/
build/
*.tgz

# Rust build outputs
target/
Cargo.lock.bak

# SP3 packaging artifacts (generated locally)
publish/
Releases/

# SP3 spike artifacts (preserved locally; never committed)
spike/

# Dependencies folder (managed at runtime, not committed)
dependencies/node/
dependencies/adb/
dependencies/node-pty/
dependencies/servy/
dependencies/node-bootstrap/
dependencies/scrcpy-server/
dependencies/*.log
.restart

# Bootstrap Node binary staged into installer payload by scripts/fetch-node.mjs.
# Build artifact, not source — re-fetched from nodejs.org on each release build.
seed/

# Superpowers brainstorm artifacts
.superpowers/

# User data
device-labels.json
ws-scrcpy-web.log
ws-scrcpy-web.log.1

# Per-session Claude Code settings (allow-list etc) — local-only, never share
.claude/

# Visual Studio local IDE state — local-only, never share
.vs/

# Playwright e2e artifacts
/test-results/
/playwright-report/
/blob-report/

# Suite bundle round-trip scratch (scripts/build-suite-bundle.mjs --verify)
/.suite-check/
