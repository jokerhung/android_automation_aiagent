import {afterEach,describe,expect,it} from "vitest";import {RateLimiter,requestKey} from "@/lib/server/rate-limiter";
describe("rate limiter",()=>{afterEach(()=>delete process.env.TRUST_PROXY);it("enforces bucket limits",()=>{const limiter=new RateLimiter();expect(limiter.check("a",2)).toBe(true);expect(limiter.check("a",2)).toBe(true);expect(limiter.check("a",2)).toBe(false)});it("ignores spoofed forwarded addresses unless proxy is trusted",()=>{const request=new Request("http://localhost",{headers:{"x-forwarded-for":"203.0.113.5"}});expect(requestKey(request,"run")).toBe("run:local");process.env.TRUST_PROXY="true";expect(requestKey(request,"run")).toBe("run:203.0.113.5")})});
