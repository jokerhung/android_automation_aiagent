import {afterEach,beforeEach,describe,expect,it} from "vitest";import type {IncomingMessage} from "node:http";import {isScrcpyUpgradeAuthorized} from "@/lib/server/scrcpy/gpl-scrcpy-bridge";import {streamSessionManager} from "@/lib/server/scrcpy/stream-session-manager";
const request=(url:string,origin="http://127.0.0.1:3000")=>({url,headers:{host:"127.0.0.1:3000",origin}} as IncomingMessage);describe("scrcpy websocket authorization",()=>{beforeEach(()=>process.env.SCRCPY_INTEGRATION="gplv3");afterEach(()=>{streamSessionManager.closeAll();delete process.env.SCRCPY_INTEGRATION});it("accepts matching origin token and immutable serial",()=>{const session=streamSessionManager.createForConnectedSerial("SERIAL-A");expect(isScrcpyUpgradeAuthorized(request(session.webSocketUrl!))).toBe(true)});it("rejects cross-origin token reuse and serial changes",()=>{const session=streamSessionManager.createForConnectedSerial("SERIAL-A"),url=session.webSocketUrl!;expect(isScrcpyUpgradeAuthorized(request(url,"http://evil.test"))).toBe(false);expect(isScrcpyUpgradeAuthorized(request(url.replace("SERIAL-A","SERIAL-B")))).toBe(false)})});
