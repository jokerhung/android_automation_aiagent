import {services} from "@/lib/server/services";export const dynamic="force-dynamic";export async function GET(){try{await services.deviceManager.listDevices();return Response.json({ok:true,data:{server:"ok",persistence:"ok",adb:"ok",modelConfigured:Boolean(process.env.OPENAI_API_KEY),stream:{mode:"snapshot",scrcpyAvailable:false}}})}catch(e){return Response.json({ok:false,error:{code:"HEALTH_FAILED",message:e instanceof Error?e.message:String(e)}},{status:503})}}
