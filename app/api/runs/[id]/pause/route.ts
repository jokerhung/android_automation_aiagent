import {services} from "@/lib/server/services";export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;services.agentRunner.pause(id);return Response.json({ok:true,data:services.sessionRepository.findRun(id)})}catch(e){return Response.json({ok:false,error:{code:"INVALID_STATE",message:e instanceof Error?e.message:String(e)}},{status:409})}}
