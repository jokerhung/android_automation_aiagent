import {localRequestGuard} from "@/lib/server/request-security";import {safeError} from "@/lib/server/logging/redaction";import {services} from "@/lib/server/services";
export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){const {id}=await params;if(!services.sessionRepository.findRun(id))return Response.json({ok:false,error:{code:"NOT_FOUND",message:"Run not found"}},{status:404});try{services.agentRunner.resume(id);return Response.json({ok:true,data:services.sessionRepository.findRun(id)})}catch(error){return Response.json({ok:false,error:{code:"INVALID_STATE",message:safeError(error)}},{status:409})}}
