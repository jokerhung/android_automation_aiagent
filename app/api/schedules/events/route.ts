import {localRequestGuard} from "@/lib/server/request-security";import {eventBus} from "@/lib/server/event-bus";const encoder=new TextEncoder();export async function GET(request:Request){const denied=localRequestGuard(request);if(denied)return denied;let unsubscribe=()=>{},heartbeat:NodeJS.Timeout;const stream=new ReadableStream<Uint8Array>({start(controller){controller.enqueue(encoder.encode(": connected\n\n"));unsubscribe=eventBus.subscribe(event=>{if(event.type.startsWith("schedule."))controller.enqueue(encoder.encode("event: "+event.type+"\ndata: "+JSON.stringify(event.data)+"\n\n"))});heartbeat=setInterval(()=>controller.enqueue(encoder.encode(": heartbeat\n\n")),15000);request.signal.addEventListener("abort",()=>{clearInterval(heartbeat);unsubscribe();try{controller.close()}catch{}},{once:true})},cancel(){clearInterval(heartbeat);unsubscribe()}});return new Response(stream,{headers:{"content-type":"text/event-stream; charset=utf-8","cache-control":"no-cache, no-transform","connection":"keep-alive"}})}
