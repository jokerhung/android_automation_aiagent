import {emailSettingsInputSchema} from "@/lib/contracts/email";import {guardEmailApi} from "@/lib/server/email/email-api-guard";import {withDraftEmailTransport} from "@/lib/server/email/email-transport";import {rateLimiter,requestKey} from "@/lib/server/rate-limiter";import {safeError} from "@/lib/server/logging/redaction";export const dynamic="force-dynamic";export async function POST(request:Request){const denied=guardEmailApi(request,true);if(denied)return denied;if(!rateLimiter.check(requestKey(request,"email-smtp-action"),5))return Response.json({ok:false,error:{code:"RATE_LIMITED",message:"Quá nhiều thao tác SMTP"}},{status:429});try{const draft=emailSettingsInputSchema.parse(await request.json());await withDraftEmailTransport(draft,transport=>transport.verify());return Response.json({ok:true,data:{verified:true,persisted:false}})}catch(error){return Response.json({ok:false,error:{code:"EMAIL_VERIFY_FAILED",message:safeError(error)}},{status:400})}}
