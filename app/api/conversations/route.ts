import {services} from "@/lib/server/services";export const dynamic="force-dynamic";export async function GET(){return Response.json({ok:true,data:services.sessionRepository.list()})}export async function POST(req:Request){let title="Cuộc trò chuyện mới";try{const body=await req.json();if(typeof body.title==="string"&&body.title.trim())title=body.title.trim().slice(0,100)}catch{}return Response.json({ok:true,data:services.sessionRepository.create(title)},{status:201})}
