import {runRequestSchema} from "@/lib/contracts/schemas";import {services} from "@/lib/server/services";export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){try{const {id}=await params;const body=runRequestSchema.parse(await req.json());await services.deviceManager.requireConnected(body.deviceSerial);services.sessionRepository.addMessage(id,"user",body.goal);const run=services.sessionRepository.createRun(id,body.deviceSerial,body.goal,body.maxSteps);services.agentRunner.start(run);return Response.json({ok:true,data:run},{status:202})}catch(e){return Response.json({ok:false,error:{code:"RUN_REJECTED",message:e instanceof Error?e.message:String(e)}},{status:400})}}
