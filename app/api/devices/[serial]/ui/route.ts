import {localRequestGuard} from "@/lib/server/request-security";import {services} from "@/lib/server/services";export const dynamic="force-dynamic";export async function GET(_:Request,{params}:{params:Promise<{serial:string}>}){try{const {serial}=await params;await services.deviceManager.requireConnected(serial);return Response.json({ok:true,data:await services.uiHierarchyService.getClickableElements(serial)})}catch(e){return Response.json({ok:false,error:{code:"UI_FAILED",message:e instanceof Error?e.message:String(e)}},{status:503})}}
