import {services} from "@/lib/server/services";export const dynamic="force-dynamic";export async function GET(_:Request,{params}:{params:Promise<{serial:string}>}){try{const {serial}=await params;await services.deviceManager.requireConnected(serial);const png=await services.adbService.run(["exec-out","screencap","-p"],{serial,timeoutMs:15000});return new Response(new Uint8Array(png.stdout),{headers:{"content-type":"image/png","cache-control":"no-store"}})}catch(e){return Response.json({ok:false,error:{code:"SNAPSHOT_FAILED",message:e instanceof Error?e.message:String(e)}},{status:503})}}
