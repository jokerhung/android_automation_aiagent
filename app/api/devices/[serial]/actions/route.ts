import {manualActionSchema} from "@/lib/contracts/schemas";import {services} from "@/lib/server/services";export async function POST(req:Request,{params}:{params:Promise<{serial:string}>}){try{const {serial}=await params;const action=manualActionSchema.parse(await req.json());return Response.json({ok:true,data:await services.deviceControlService.execute(serial,action)})}catch(e){return Response.json({ok:false,error:{code:"ACTION_REJECTED",message:e instanceof Error?e.message:String(e)}},{status:400})}}
